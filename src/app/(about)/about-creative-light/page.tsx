import AboutCreativeMain from '@/pages/about/about-creative/AboutCreativeMain';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Forward Deployed Engineer | About Creative light",
};

const page = () => {

    return (
        <AboutCreativeMain />
    );
};

export default page;